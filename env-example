#
# Copy this file into a new file called ".env" and modify it to contain
# correct values for environment variables: SONARQUBE_EXTERNAL_HOST_PORT, 
# SONARQUbE_INTERNAL_DOCKER_PORT and PSQL_DATABASE_NAME
# NOTE: .env file should not be added to git as its contents are sensitive
#
SONARQUBE_EXTERNAL_HOST_PORT=8090
SONARQUBE_INTERNAL_DOCKER_PORT=9000

PSQL_DATABASE_NAME=sonar
